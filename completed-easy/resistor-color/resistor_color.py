colors_encoded = ["black", "brown", "red", "orange", "yellow", "green", "blue", "violet", "grey", "white"]

def color_code(color):
    return colors_encoded.index(color)

def colors():
    return colors_encoded
